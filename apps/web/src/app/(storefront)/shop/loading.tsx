import { LogoLoaderScreen } from "@/components/brand/logo-loader-screen";

/** Shown while the listing and its facets are fetched. */
export default function ShopLoading() {
  return <LogoLoaderScreen label="Loading products" />;
}
