import { LogoLoaderScreen } from "@/components/brand/logo-loader-screen";

/** Shown while any storefront route streams in. */
export default function StorefrontLoading() {
  return <LogoLoaderScreen label="Loading" />;
}
